import React from "react";

const Home = props => (
  <div>
    <h1 className="cover-heading">Your Secrets, Your Profit</h1>
    <p className="lead">
      Whisper allows users to post and sell secrets. Secrets are what make the
      world go round.
    </p>
  </div>
);

export default Home;
